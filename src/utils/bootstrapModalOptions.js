/**
 * Bootstrap 5 Modal 建構選項（與 `new Modal(element, options)` 第二參數相同）
 * @see https://getbootstrap.com/docs/5.3/components/modal/#options
 */

/** 禁止以 ESC 關閉（後台新增／編輯／刪除確認等） */
export const MODAL_NO_KEYBOARD = Object.freeze({ keyboard: false });
